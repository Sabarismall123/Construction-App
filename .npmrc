# Ensure platform-specific packages are installed correctly
# This prevents Windows-specific packages from being used on Linux (Vercel)
legacy-peer-deps=false
prefer-offline=false
audit-level=moderate
