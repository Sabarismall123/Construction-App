import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.construction.app',
  appName: 'Construction Management',
  webDir: 'dist'
};

export default config;
